import { AppPageHeader } from "@/components/shell/app-page-header";

export default function ResumeEditorPage() {
  return (
    <AppPageHeader
      title="Resume editor"
      description="Standalone resume editor."
    />
  );
}
